# runtime

This directory is intentionally empty in the public repository.

Place local-only inputs here when building the project privately:

- `festival.exe`
- `wp_runtime_lib/`

Do not commit proprietary runtime files to the public repository.
