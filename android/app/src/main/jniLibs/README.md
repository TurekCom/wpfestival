# jniLibs

Place locally built native libraries here for private builds, for example:

- `arm64-v8a/libwpfestival_exec.so`
- `x86_64/libwpfestival_exec.so`

These binaries are intentionally excluded from the public repository.
